<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Smart Building | 🔐 Reset Password</title>
  <link rel="stylesheet" href="/styles.css" />
  <style>
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background: var(--bg, #f4f6f9);
      font-family: 'Segoe UI', sans-serif;
    }
    .reset-box {
      background: white;
      padding: 2rem;
      border-radius: 1rem;
      box-shadow: 0 0 20px rgba(0,0,0,0.1);
      width: 350px;
    }
    .reset-box h2 {
      margin-bottom: 1rem;
      text-align: center;
    }
    label {
      font-size: 0.9rem;
      display: block;
      margin: 0.5rem 0 0.2rem;
    }
    input {
      width: 100%;
      padding: 0.7rem;
      margin-bottom: 0.7rem;
      border: 1px solid #ccc;
      border-radius: 0.5rem;
      font-size: 1rem;
    }
    button {
      background: #2c3e50;
      color: white;
      border: none;
      padding: 0.8rem 1rem;
      width: 100%;
      border-radius: 0.5rem;
      cursor: pointer;
      font-size: 1rem;
    }
    .msg {
      margin-top: 1rem;
      font-size: 0.9rem;
      text-align: center;
    }
    .msg.success {
      color: green;
    }
    .msg.error {
      color: red;
    }
    .back {
      text-align: center;
      margin-top: 1rem;
    }
    .back a {
      text-decoration: none;
      color: #2c3e50;
      font-size: 0.9rem;
    }
  </style>
  <script>
    function validateForm() {
      const pwd = document.getElementById('pwd').value;
      const confirm = document.getElementById('confirm').value;
      if (pwd !== confirm) {
        alert('❌ Passwords do not match!');
        return false;
      }
      return true;
    }
  </script>
</head>
<body>
  <div class="reset-box">
    <h2>🔐 Reset Password</h2>
    <form method="POST" action="/reset/<%= token %>" onsubmit="return validateForm()">
      <label for="pwd">New Password</label>
      <input id="pwd" type="password" name="password" placeholder="Enter new password" required autocomplete="new-password" />

      <label for="confirm">Confirm Password</label>
      <input id="confirm" type="password" name="confirm" placeholder="Confirm new password" required autocomplete="new-password" />

      <button type="submit">Reset Password</button>
    </form>

    <% if (error) { %>
      <div class="msg error"><%= error %></div>
    <% } %>
    <% if (success) { %>
      <div class="msg success"><%= success %></div>
    <% } %>

    <div class="back">
      <a href="/login">← Back to Login</a>
    </div>
  </div>
</body>
</html>
